# Rubik Solver Demo - GitHub Pages

Simple single-page demo that shows a 3x3 cube editor and frame-by-frame animation of a move sequence. To deploy: create a new GitHub repo, add these files to root, commit to main, then enable GitHub Pages (main branch / root).
